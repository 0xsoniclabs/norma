Name: Two Nodes Test
Description: >-
  Runs two validator nodes with different images to
  verify multi-node consensus and compatibility.

Scenario:
  - startNode: validator-0
    type: validator

  - waitFor: 10s

  - startNode: validator-1
    type: validator
    imageName: "sonic:v2.1.6"

  - waitFor: 10s
