Name: Single Node Test
Description: >-
  Runs a minimal network with a single validator to
  verify basic block production.

Scenario:
  - startNode: validator-latest
    type: validator
    instances: 1

  - waitFor: 10s
